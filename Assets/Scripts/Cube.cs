using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cube : MonoBehaviour
{
    [HideInInspector] public int CubeID = -1;

    // Start is called before the first frame update
    void Start()
    {
    
    }
}
